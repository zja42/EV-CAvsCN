# EV-CAvsCN

Rendered report for a Canada vs China electric vehicle survey analysis.

## Files

- `index.html` - rendered project report exported from the analysis notebook

## View Locally

Open `index.html` in a browser, or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

